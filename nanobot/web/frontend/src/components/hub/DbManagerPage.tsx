import { ComingSoon } from "./ComingSoon";

export function DbManagerPage() {
  return (
    <ComingSoon
      title="Bancos de Dados"
      description="Conecte SQL/NoSQL para consultas em tempo real. Disponível em breve — use APIs MCP no lugar."
    />
  );
}
